# sunseg

ระบบพยากรณ์การเกิด solar flare ที่ประกอบด้วยสองส่วน: U-Net แบ่งส่วน active region จากภาพ
magnetogram เต็มดวง และโมเดลพยากรณ์สี่ตัว (LSTM, TCN, Transformer, DA-RNN) พยากรณ์โอกาสเกิด
flare ≥ M1.0 ภายใน 24 ชม. จากลำดับเวลาราย active region **เป้าหมายของงานวิจัย (ตั้งแต่ 2026-09-24)
คือเปรียบเทียบโมเดลเพื่อหาตัวที่ให้ผลดีที่สุด** และผู้ชนะ (LSTM + V3) เป็น **โมเดลหลัก** ที่ทำนายระดับคลาส <M / M / X ไม่ใช่การพิสูจน์ว่าข้อมูลหลายความยาวคลื่นช่วยหรือไม่ —
ชุด feature เป็นเพียงตัวเลือกหนึ่งของโมเดล เอกสารนี้เป็น**อภิธานศัพท์เท่านั้น** — การตัดสินใจเชิงเทคนิค
อยู่ใน spec และ README

## Language

### การระบุตัวตนของ active region

โปรเจกต์นี้มีตัวระบุ active region อยู่สามระบบที่ไม่ตรงกัน การสับสนระหว่างสามคำนี้เป็นที่มาของ
บั๊กที่หายากที่สุดในโค้ดฐานนี้

**HARP**:
หมายเลขแพตช์ที่ HMI ติดตามเอง (`HARPNUM`) เป็นตัวระบุที่ทุกแถวของ dataset ผูกอยู่ และเป็น
กุญแจของการแบ่ง train/val/test
_Avoid_: patch, region id

**detection**:
กลุ่มพิกเซลที่ต่อเนื่องกันซึ่ง U-Net ทำนายว่าเป็น active region ในเฟรมหนึ่ง **ยังไม่มีตัวตน**
จนกว่าจะถูกจับคู่เข้ากับ HARP
_Avoid_: AR (กำกวม — ดูด้านล่าง), blob, component

**NOAA AR**:
หมายเลขที่ NOAA ตั้งขึ้นอย่างเป็นอิสระจาก HARP ใช้เฉพาะการแสดงผลบนหน้าเว็บและการเชื่อมกับ
รายการ flare เท่านั้น ห้ามใช้เป็นกุญแจของ dataset
_Avoid_: AR number

**active region**:
ปรากฏการณ์ทางฟิสิกส์จริงบนดวงอาทิตย์ ใช้คำนี้เมื่อพูดถึงตัวปรากฏการณ์ ไม่ใช่ตัวระบุ — เมื่อ
หมายถึงตัวระบุให้เลือกใช้ HARP, detection หรือ NOAA AR ให้ตรงตัว
_Avoid_: AR โดด ๆ ในโค้ดหรือเอกสารที่ความหมายอาจอ่านเป็นตัวระบุได้

**แผนที่ระบุตัวตน (identity map)**:
ภาพ raster ที่แต่ละพิกเซลเก็บ `HARPNUM` ของ HARP ที่ครอบพิกเซลนั้น ใช้บอกว่า detection หนึ่ง
ตรงกับ HARP ดวงไหน
_Avoid_: label map, mask (mask เป็นแบบ binary ไม่มีตัวตน)

### หน่วยของข้อมูล

**sample**:
คู่ `(HARPNUM, issue_time)` หนึ่งคู่ — หน่วยที่โมเดลรับเข้าและออกคำทำนายหนึ่งค่า
_Avoid_: row, record, event

**issue_time**:
จุดเวลาสุดท้ายของหน้าต่างลำดับ คือช่วงเวลาที่คำพยากรณ์ถูกออก ทุกอย่างก่อนหน้านี้คือ feature
ทุกอย่างหลังจากนี้คือ label
_Avoid_: timestamp, t0, prediction time

**หน้าต่างพยากรณ์ (horizon)**:
ช่วง 24 ชม. หลัง `issue_time` ที่ label ถามว่าจะเกิด flare ≥ M1.0 หรือไม่

### รายการ flare

**แคตตาล็อกของโมเดล**:
รายการ flare ใน `data/interim/flares.parquet` (เก็บเป็นคู่ flare–HARP) ที่ใช้ตัดสิน label ทุกดวง
คลาสในนี้คือคลาสที่นับว่า "≥ M1.0" จริง — ปี ≤ 2017 เป็นสเกล NGDC เดิม (คูณ 0.7) จึงอาจต่ำกว่า
ตัวเลขในแหล่งอื่นราว 1.43 เท่า
_Avoid_: GOES catalog (กำกวม — มีหลายแคตตาล็อกที่มาจาก GOES)

**ตำแหน่ง flare**:
พิกัด heliographic (lat, lon) ของ flare บนจานสุริยะ ในหน้าเว็บยืมมาจากแคตตาล็อกของ PositionFlare
แล้วจับคู่ทีละดวงเข้ากับแคตตาล็อกของโมเดลด้วยเวลาพีค — **ไม่ใช่ feature ของโมเดล** ใช้แสดงผลเท่านั้น
ดวงที่หาคู่ไม่เจอหรือ PositionFlare ไม่ทราบตำแหน่ง คือ "ไม่มีพิกัด" ห้ามวาดที่ (0, 0)
_Avoid_: location (ชื่อคอลัมน์สตริงแบบ `S12W40` ในรายงาน NGDC — คนละรูปแบบ)

### ความเข้มแสงและชั้นบรรยากาศ

**ช่อง (channel)**:
ความยาวคลื่นของ AIA หนึ่งค่าจากสามค่า (1600 / 304 / 171 Å) ซึ่งแทนชั้นบรรยากาศหนึ่งชั้น
(โฟโตสเฟียร์ / โครโมสเฟียร์ / โคโรนา)
_Avoid_: **intensity layer** — คำนี้กำกวมและชนกับ `layer` ในหน้าเว็บ (ดูด้านล่าง) เมื่อหมายถึง
"3 ชั้นบรรยากาศ" ให้เขียนว่า 3 ช่อง (ความหมายที่เจ้าของงานยืนยันแล้วเมื่อพูดว่า "3 intensity
layers" คือ 3 ความยาวคลื่น ไม่ใช่ 3 feature)

**intensity feature**:
ค่าสถิติหนึ่งค่า (มัธยฐาน, p95, ฯลฯ) ของความสว่างที่ normalise ด้วย quiet Sun แล้ว วัดจาก
พิกเซลใน mask ของ detection หนึ่ง ในช่องหนึ่ง — "3 ช่อง × 2 สถิติ" จึงเท่ากับ 6 intensity feature
ไม่ใช่ 3

**quiet Sun**:
พิกเซลบนจานดวงอาทิตย์ที่อยู่นอก mask ทุกอัน ใช้เป็นตัวหารเพื่อให้ความสว่างเทียบกันข้ามปีได้
โดยไม่ต้องแก้ instrument degradation

**layer** (เฉพาะฝั่งหน้าเว็บ):
ภาพที่กำลังแสดงในแผงดูเฟรม — magnetogram, mask หรือช่อง AIA หนึ่งช่อง **คนละความหมาย
กับชั้นบรรยากาศ** และเป็นเหตุผลที่ห้ามใช้คำว่า "intensity layer"

### โมเดลพยากรณ์

**โมเดลพยากรณ์ (forecast model)**:
หนึ่งรายการใต้ `models:` ของ `configs/forecast.yaml` — สถาปัตยกรรมหนึ่งตัวพร้อม hyperparameter
ชุดเดียวที่ระบบใช้งานจริงบนหน้าเว็บ ระบุด้วยชื่อ (`lstm`, `tcn`, `transformer`, `darnn`) ซึ่งเป็น
ชื่อเดียวกับ `--model` ของสคริปต์เทรน, `artifacts/models/<ชื่อ>.pt` และ `?model=` ของ API
ทุกตัวใช้ dataset ชุดเดียวกัน (18 SHARP × 24 ชม.) และเทรนคู่กับ baseline ทุกครั้ง
**ไม่ใช่เซลล์ของงานเปรียบเทียบ** — hyperparameter ของ TCN/Transformer/DA-RNN ยืมมาจากการค้นหาของ
งานเปรียบเทียบ แต่เทรนบน dataset ของ production คนละชุดกัน ตัวเลขจึงเทียบกับตารางตรง ๆ ไม่ได้
_Avoid_: "LSTM" เมื่อหมายถึงโมเดลพยากรณ์ทั่วไป, production model (มีหลายตัวแล้ว ไม่ได้ชี้ตัวเดียว)

**โมเดลปริยาย (default model)**:
`default_model` ของ `configs/forecast.yaml` (ตอนนี้คือ `lstm`) — ตัวที่ API ใช้เมื่อไม่ระบุ `?model=`
และตัวที่แผงความเสี่ยงรายชั่วโมงเปิดมาเจอก่อน ผู้ใช้สลับเป็นตัวอื่นได้ด้วยปุ่มเลือกโมเดล
**ไม่ใช่โมเดลหลัก** — เป็นแค่ตัวเริ่มต้นของโมเดลพยากรณ์รายชั่วโมง

**โมเดลหลัก (main model)**:
เซลล์ (LSTM, V3) ของงานเปรียบเทียบ ตั้งแต่ 2026-09-24 — ตัวที่หน้าเว็บ รายงาน และเอกสารยกเป็นผลงานหลัก
ของโปรเจค ประกอบด้วย ensemble ทุก seed **สองชุด** ของเซลล์เดียวกัน: ชุดที่เทรนด้วย label ≥M1.0
(`artifacts/model_comparison`) และชุดที่เทรนด้วย label ≥X1.0 (`artifacts/model_comparison_x`) ตั้งค่าที่
`class_forecast:` ของ `configs/forecast.yaml` · อ่าน study dataset (ราย 12 ชม.) ไม่ใช่ sequence ราย 1 ชม.
ของโมเดลพยากรณ์ จึงมี API แยก (`/api/class-forecast`)
_Avoid_: "LSTM" เฉย ๆ (ชนกับ `models.lstm` รายชั่วโมง), "anchor" (anchor คือ LSTM + V0), flagship

**ระดับคลาส (class level)**:
คำตอบหนึ่งค่าของโมเดลหลักต่อ sample — `X` (≥X1.0), `M` (≥M1.0 แต่ไม่ถึง X) หรือ `<M` (ไม่มีระดับไหนเตือน)
คือระดับสูงสุดที่ ensemble ของระดับนั้นเตือน · **`<M` ไม่ได้แปลว่าจะเกิด C** — ไม่มีแบบจำลอง ≥C1.0
ระดับจริงของ sample มาจาก label สองชุดเดียวกับที่เทรน
_Avoid_: "คลาส" เฉย ๆ (ชนกับคลาส GOES ของ flare หนึ่งดวง เช่น M2.3), "C" แทน `<M`

**จุดทำงาน (mode)**:
กติกาที่ตัดสินว่าระดับ X ของโมเดลหลักเตือนเมื่อไร มีสองจุด — `sensitive` (เตือนไว, ปริยาย: เสียงข้างมากของ seed
ที่ใช้ threshold ให้ TSS สูงสุด) และ `strict` (ระมัดระวัง: ความน่าจะเป็นเฉลี่ยข้าม seed ≥ threshold เดียวที่เลือกบน
val ให้ HSS 3 คลาสสูงสุด) · ระดับ M ใช้กติกาเดียวกันทั้งสองจุด
_Avoid_: threshold (หมายถึงค่าตัวเลขหนึ่งค่า ไม่ใช่กติกา), operating point ในข้อความภาษาไทย

### การทดลองเปรียบเทียบ

ตารางเปรียบเทียบมีสองแกน (สถาปัตยกรรม × แบบ) แต่**คำถามหลักมีข้อเดียว: เซลล์ไหนดีที่สุด**
(ตั้งแต่ 2026-09-24 — ก่อนหน้านั้นคำถามหลักคือ "intensity/X-ray ช่วยไหม" ซึ่งตอนนี้เป็นการวิเคราะห์
ประกอบในภาคผนวก: Δ ตามแบบ และ interaction)

**แบบ (variant)**:
ชุด feature ที่ตั้งชื่อไว้หนึ่งชุด นิยามเป็นรายชื่อ column ที่เลือกจาก dataset ก้อนเดียวกัน — ทุกแบบ
ใช้แถวข้อมูลชุดเดียวกันเสมอ เป็นแกนที่หนึ่งของตารางเปรียบเทียบ และเป็นตัวเลือกหนึ่งของโมเดลเหมือน
hyperparameter ไม่ใช่คำถามวิจัยแยก
_Avoid_: model (กำกวมกับ checkpoint — และตอนนี้ชนกับสถาปัตยกรรมด้วย), configuration, arm

**สถาปัตยกรรม (architecture)**:
โครงของโมเดลหนึ่งแบบที่ตั้งชื่อไว้ (LSTM, TCN, Transformer, DA-RNN) เป็นแกนที่สอง
ของตารางเปรียบเทียบ — นิยามที่ `configs/study/architectures.yaml` และประกอบขึ้นผ่าน
`sunseg.models.forecast.registry` การเลือกวิธี pooling (attention pooling หรือ mean/last-step) เป็น
hyperparameter ของแต่ละสถาปัตยกรรม ไม่ใช่ส่วนหนึ่งของชื่อ
_Avoid_: model (ชนกับ checkpoint ที่เทรนเสร็จแล้ว, กับคีย์ `model:` ใน forecast.yaml และกับ **โมเดลพยากรณ์**), arch, backbone

**เซลล์ (cell)**:
คู่ (สถาปัตยกรรม, แบบ) หนึ่งคู่ — หน่วยที่มีตัวเลขผลหนึ่งชุดต่อ seed และเป็น**ผู้สมัครหนึ่งตัวของ
การหาโมเดลที่ดีที่สุด** ตารางหลักมี 16 เซลล์ (4 × V0–V3) ในเอกสารสำหรับผู้อ่านภายนอกเรียกว่า
"โมเดล (สถาปัตยกรรม + ชุด feature)" ได้
_Avoid_: run (หมายถึงการเทรนหนึ่งครั้ง คือหนึ่ง seed ของหนึ่งเซลล์), config

**เซลล์ที่ดีที่สุด (best cell)**:
เซลล์ที่ TSS เฉลี่ยบน test (ข้าม seed) สูงสุด — อันดับ 1 ของ `sunseg.study.summary.rank_cells`
ค่าของมันเฟ้อขึ้นจากการเลือกตัวสูงสุด (winner's curse) จึงต้องรายงานคู่กับจำนวนเซลล์ที่ **แยกจาก
อันดับ 1 ไม่ได้** และอันดับบน validation เสมอ
_Avoid_: "โมเดลที่ชนะ" เมื่อยังมีเซลล์ที่แยกจากอันดับ 1 ไม่ได้

**แยกจากอันดับ 1 ไม่ได้ (tied with best)**:
ระยะห่างจากเซลล์ที่ดีที่สุด (จับคู่ seed) ไม่เกิน SD ของระยะห่างนั้นเอง — เกณฑ์เดียวกับตัวหนาในรายงาน
ไม่ใช่การทดสอบนัยสำคัญ

**anchor**:
เซลล์ (LSTM, 18 SHARP) — จุดที่สองแกนตัดกัน ทุก Δ ในภาคผนวกของ `report.md` สืบย้อนถึงตัวนี้ได้
(ไม่เกี่ยวกับการจัดอันดับ — อันดับเทียบกับเซลล์ที่ดีที่สุด ไม่ใช่ anchor)
ตั้งแต่ 2026-09-23 ใช้ hyperparameter ชุดเดียวกับ `models.lstm` ใน `configs/forecast.yaml` (ทั้งคู่มาจากการค้นหา)
ส่วนค่าตั้งมือเดิมของระบบคือแถวเสริม `lstm_production` ("LSTM (untuned)") ใต้ตาราง
รายงานฉบับ Ver4 ไม่ยึด anchor — แสดงผลต่างระหว่างสถาปัตยกรรมครบทุกคู่แทน

**control**:
คำนี้**กำกวมแล้ว**ตั้งแต่ตารางมีสองแกน ห้ามใช้เดี่ยว ๆ — ให้ระบุว่าหมายถึง **แบบอ้างอิง**
(18 SHARP ของสถาปัตยกรรมนั้นเอง) **สถาปัตยกรรมอ้างอิง** (LSTM ของแบบนั้นเอง)
หรือ **anchor**

**Δ ตามแบบ**:
ผลต่างของเซลล์เทียบกับแบบอ้างอิงของสถาปัตยกรรมเดียวกัน จับคู่ seed — ตอบว่า feature ที่เพิ่ม
เข้ามาช่วยอะไร (ภาคผนวก)

**Δ ตามสถาปัตยกรรม**:
ผลต่างของเซลล์เทียบกับสถาปัตยกรรมอ้างอิงของแบบเดียวกัน จับคู่ seed — ตอบว่าสถาปัตยกรรม
ไหนดีกว่า

**interaction**:
Δ ตามแบบ ของสถาปัตยกรรมหนึ่ง ลบด้วย Δ ตามแบบ ของ LSTM — ตอบว่าชุด feature หนึ่งให้ผลต่างกัน
ตามสถาปัตยกรรมไหม (ภาคผนวก — เคยเป็นคำถามหลักก่อน 2026-09-24)

**baseline**:
คำนี้**ถูกใช้ไปแล้ว**ในโค้ดฐานนี้ หมายถึงโมเดล logistic regression ที่เทรนคู่กับโมเดลพยากรณ์ทุกตัว
(`baseline_prob`) เมื่อพูดถึงแบบที่ใช้ X-ray ล้วนในงานเปรียบเทียบ ให้เรียกว่า **activity baseline**
ให้เต็มคำเสมอ เพื่อไม่ให้ชนกัน

**activity baseline**:
แบบที่ใช้ฟลักซ์รังสีเอกซ์ทั้งดวงอย่างเดียว วัดว่าทักษะการพยากรณ์ที่เห็นมาจากการรู้ระดับความ
คึกคักของดวงอาทิตย์เฉย ๆ กี่ส่วน ไม่ใช่คู่แข่งเท่าเทียมของแบบอื่น เพราะค่าของมันเท่ากันสำหรับ
ทุก HARP ณ เวลาเดียวกัน

# Data — ห้าม commit (ใหญ่มากและดาวน์โหลดใหม่ได้)
# ต้องยึด root (/data/) ไม่งั้นจะจับ backend/src/sunseg/data/ (แพ็กเกจโค้ด) ไปด้วย —
# เคยเกิดมาแล้วจริง: ทั้งแพ็กเกจไม่เคยถูก commit เลยตั้งแต่ initial commit เพราะบรรทัดนี้
/data/
/artifacts/
/runs/
*.fits
*.fits.gz
*.npy
*.npz
*.parquet
*.pt
*.pth
*.ckpt

# Environment
.venv/
.env
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# Notebooks
.ipynb_checkpoints/

# Frontend (Vite + React) — build output, ไม่ commit node_modules/dist
frontend/node_modules/
frontend/dist/

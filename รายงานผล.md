# รายงานผล — แปลง frontend เป็น React, แยก Docker container, และแก้ไขตาม code review

วันที่: 2026-09-07
ขอบเขต: `frontend/`, `backend/app/main.py`, `Dockerfile` (backend + frontend), `docker-compose.yml`

---

## 1. แปลง frontend จาก vanilla JS เป็น React + Vite

**ก่อน**: `frontend/app.js` (2,441 บรรทัด) + `index.html` + `style.css` — ไม่มีขั้นตอน build, ทุกอย่างเขียนด้วยมือ ผูกกับ DOM ตรงๆ

**หลัง**: โปรเจค Vite + React ใต้ `frontend/src/` จัดโครงสร้างใหม่ตามแผงของ dashboard

```
frontend/src/
  main.jsx, App.jsx
  state/
    AppProvider.jsx   ศูนย์กลาง state ของแอป (เทียบเท่า `state` object เดี่ยวเดิม)
    AppContext.js
  components/
    Topbar.jsx, Banner.jsx, Footer.jsx, PresetsBar.jsx, FilterBar.jsx, PlotlyChart.jsx
    Hero/             Hero, DiskChart (จานสุริยะ), HeroProtonChart
    Dashboard/         FrameCard, ExtractionCard, XrayCard, ProtonCard, RiskCard,
                        HarpListCard, AttentionCard
    About/             AboutSection, ConfusionMatrix
  lib/                api.js, theme.js, format.js, plotConstants.js, motion.js,
                       asyncState.js, proton.js, csv.js
  hooks/              useChrome.js, useReveal.js, useCountUp.js
  styles.css          CSS เดิมยกมาทั้งไฟล์ — class name ไม่เปลี่ยน
```

**สิ่งที่คงไว้ครบ**: ช่วงเวลาเดียวขับทุกแผง, ตาราง HARP กรอง/เรียง/ค้นหา, กราฟความเข้มแสงราย AR
พร้อมปุ่ม snap-to-frame, GOES X-ray light curve ที่อัปเกรดเป็นข้อมูลจริงรายนาทีแบบ progressive,
แผงโปรตอนพร้อม hover-preview + click-to-pin, จานสุริยะพร้อม filter chip + tooltip + ring, ตัวเลือก
เลเยอร์ภาพ + timelapse + คีย์ลัดคีย์บอร์ด, risk gauge, attention chart, confusion matrix พร้อม
slider + drill-down, export CSV, scroll-reveal/scroll-spy

**Plotly**: ยังโหลดจาก CDN เหมือนเดิม (ไม่ bundle เข้า npm) — ไฟล์ใหญ่และมีกราฟหลายชนิดที่ใช้ไม่ถึง

---

## 2. แยก Docker container เป็น backend และ frontend

**ก่อน**: container เดียว — FastAPI เสิร์ฟทั้ง API และไฟล์ static ของ frontend

**หลัง**: สอง service ใน `docker-compose.yml`

| Service | Image | พอร์ต | หน้าที่ |
|---|---|---|---|
| `backend` | `backend/Dockerfile` | 8000 | FastAPI — API ล้วน ไม่รู้จักไฟล์ frontend เลย |
| `frontend` | `frontend/Dockerfile` | 3000 | nginx เสิร์ฟ React build, proxy `/api`, `/docs`, `/openapi.json` ไปที่ backend |

เปิดใช้งาน: `docker compose up --build` → http://localhost:3000 (เอกสาร API ที่ http://localhost:8000/docs)

**หมายเหตุพอร์ต**: เดิมตั้งใจใช้ 8080 แต่ชนกับโปรเจคอื่น (`position_flare-frontend`) ที่รันอยู่ในเครื่องนี้แล้ว จึงเปลี่ยนเป็น 3000

**ปัญหาที่เจอระหว่าง build และวิธีแก้**:
1. `backend/pyproject.toml` มี `readme = "../README.md"` — hatchling รุ่นใหม่ปฏิเสธ readme path ที่ชี้ออกนอก project directory เสมอ แก้ด้วยการ pin `hatchling==1.18.0` ใน Dockerfile (ไม่แตะ `pyproject.toml`)
2. ไม่มี `.dockerignore` — `node_modules` ที่ build บน Windows host เกือบหลุดเข้าไปทับของที่ build สดในคอนเทนเนอร์ Linux (native binary ของ esbuild ไม่ตรง platform) เพิ่ม `.dockerignore` ทั้ง root และ `frontend/`

---

## 3. Code review (`/code-review`) — สองแกนคู่ขนาน

รีวิว working tree เทียบกับ commit `3c08d97` (จุดก่อนเริ่มงานทั้งหมด) แยกเป็น **Standards** (คอนเวนชันของ repo + code smell baseline) และ **Spec** (พฤติกรรมเทียบกับ `app.js` เดิม)

สรุปยอดตอนรีวิว: Standards 9 ประเด็น, Spec 7 ประเด็น (เหลือจริง 6 หลังหักข้อที่ agent ตั้งธงผิด — โหมด dense
ของกราฟ X-ray ไม่ใช่ scope creep แต่เป็นงานที่มีอยู่แล้วใน working tree ก่อนเริ่มแปลง)

ผลรีวิวทั้งหมดถูกสรุปเป็น prompt สำหรับแก้ไขไว้ที่ [`code-review-fixes.md`](code-review-fixes.md)

---

## 4. แก้ไขตามผลรีวิว

### หมวด A — บั๊กพฤติกรรม (แก้ครบ 6 ข้อ ยืนยันด้วย browser จริง)

| # | ปัญหา | วิธีแก้ | ยืนยันผล |
|---|---|---|---|
| A1 | pin/unpin จุด flare บนจานสุริยะพัง (stale closure ใน event handler ของ Plotly) | เพิ่ม `pinnedFlareRef`/`goesRef` ใน `AppProvider.jsx` ให้ `selectFlareEvent`/`previewFlareEvent` อ่านค่าล่าสุดเสมอไม่ว่าจะถูกเรียกจาก closure รุ่นไหน | ทดสอบสด: คลิกจุด → ตรึง, hover จุดอื่น → **ไม่เปลี่ยน**, คลิกจุดเดิมซ้ำ → ปลดตรึงถูกต้อง |
| A2 | สถานะ loading ล้างกราฟที่วาดไว้แล้ว (X-ray, จานสุริยะ, โปรตอน กะพริบว่างทุกครั้งที่โหลดใหม่) | คงข้อมูลเก่าไว้ระหว่างโหลด (`loadingAsync` เก็บ data เดิม) + เปลี่ยนเงื่อนไข `empty` ของกราฟให้ดูจาก "มีข้อมูลอยู่ไหม" แทนการดูจาก status | ทดสอบสด: กดปุ่ม "แสดงข้อมูล" ซ้ำๆ ไม่กะพริบเป็นค่าว่างแล้ว |
| A3 | การ์ด Proton ค้างที่ opacity:0 มองไม่เห็น (mount ทีหลังแบบ async, พลาด `querySelectorAll` รวมศูนย์ตอน mount) | เขียน `useReveal()` hook ใหม่แบบ callback ref ผูกทีละ component (แทนการ query รวมศูนย์ครั้งเดียวตอน app mount) | ตรวจโค้ด + build ผ่าน (ต้องมี `SUNSEG_PROTON_DIR` ถึงจะเห็นการ์ดนี้จริง — Docker ไม่มีคลังข้อมูลนี้) |
| A4 | error ตอนโหลดรายการ HARP หายเงียบ ตารางค้าง "กำลังโหลด…" ตลอดไป | เพิ่ม `harpsError` state แสดงข้อความ error แทน | ตรวจโค้ด + build ผ่าน |
| A5 | ยิง `/api/segment` ซ้ำเมื่อ backend ถอย layer (ขอ 304 แต่เฟรมไม่มีข้อมูล) | แยก "คำขอที่กำลังจะยิง" ออกจาก "คำขอที่เพิ่งสำเร็จ" ด้วย `lastFetchRef` ข้าม fetch ถ้าตรงกับที่เพิ่งได้มาแล้ว | ทดสอบสด: สลับ layer pill → เห็น 1 request ใน network log |
| A6 | ข้อความ "mask U-Net (Segmentation)" ถูกเปลี่ยนเป็น "mask ที่ U-Net ทำนาย" โดยไม่ตั้งใจ | คืนข้อความเดิม | ตรวจกับต้นฉบับใน git history ตรงกัน |

หมายเหตุ: พบว่าข้อเสนอเดิมเรื่อง `harpCount` (บอกว่าเดิมแสดงบรรทัดนับเสมอ) **รายงานผิด** — ตรวจกับ
`git show 3c08d97:frontend/app.js` แล้วพบว่าโค้ดเดิมก็ปล่อยว่างตอนยังไม่มีข้อมูลเหมือนกัน จึงไม่ได้แก้จุดนี้

### หมวด B — คุณภาพโค้ดและคอนเวนชัน (แก้ครบ 9 ข้อ)

- **B1** — `ProtonCard.jsx`: เลิกประกอบ HTML string ยัดผ่าน `dangerouslySetInnerHTML` เขียนเป็น JSX ตรงๆ
- **B2** — คืนคอมเมนต์ "ทำไม" ที่หายไประหว่างแปลง (คัดจาก git history ของ `app.js` เดิมจริงๆ ไม่ใช่เดา): docblock อธิบายสถาปัตยกรรมทั้งหน้าใน `App.jsx`, เหตุผลเรื่องช่วงเวลาร่วม/การผูกกันของการ์ดใน `AppProvider.jsx`, field comment ของ confusion matrix ใน `ConfusionMatrix.jsx`
- **B3** — แก้คอมเมนต์ที่ชี้ไปยัง `app.js` ซึ่งถูกลบไปแล้ว (`plotConstants.js`, `api.js`, `RiskCard.jsx`, `format.js`, `Hero.jsx`) ด้วยการย้ายเหตุผลจริงมาเขียนไว้ตรงนั้นแทน
- **B4** — เพิ่มคอมเมนต์อธิบายเหตุผลที่ Plotly โหลดจาก CDN ไม่ bundle เข้า npm
- **B5** — ลดโค้ดซ้ำ: รวมการคำนวณช่วงแกนของกราฟโปรตอนเป็น `lib/proton.js`, รวม `REDUCED_MOTION` เป็นแหล่งเดียวใน `lib/motion.js`, `RiskCard.jsx` เหลือ shell เดียวแทนที่จะก็อป 4 ครั้ง
- **B6** — ทำ `lib/asyncState.js` (`idleAsync`/`loadingAsync`/`readyAsync`/`errorAsync`) ให้ state รูปแบบ `{status, data, error}` เหมือนกันทั่วแอป ใช้ใน `AppProvider.jsx` และ `ConfusionMatrix.jsx` (เดิม `ConfusionMatrix` ใช้ sentinel แปลกๆ เช่น `"loading"` string)
- **B7** — เปิด `restyle()` ออกจาก `PlotlyChart` ผ่าน `forwardRef`/`useImperativeHandle` แทนที่ `XrayCard.jsx` จะเจาะ `document.getElementById` ตรงๆ
- **B8** — เปลี่ยนชื่อ `selectHarpInternal`/`applyRangeInternal` เป็น `selectHarp`/`applyRange` (เดิมชื่อบอกว่า internal แต่ export ออกไปจริง), เปลี่ยนชื่อตัวแปร `dice` → `diceText` ใน `FrameCard.jsx` (เก็บสตริงไม่ใช่ตัวเลข)
- **B9** — แยกการสร้างสตริง CSV ออกเป็น pure function ใน `lib/csv.js` ให้ `XrayCard.jsx` เป็นคนสั่งดาวน์โหลดเอง แทนที่จะอยู่ปนกับ state ใน `AppProvider.jsx`

---

## 5. การยืนยันผล

- `npm run build` ผ่านสะอาดทุกรอบหลังแก้ (ล่าสุด: `dist/assets/index--wbepvWp.js`, 212.20 kB)
- Docker: rebuild + restart `sunseg-frontend` แล้ว ทั้งสอง container รันอยู่ที่พอร์ต 3000/8000
- ทดสอบผ่าน headless browser (Playwright) จริงต่อทั้งสภาพแวดล้อม Docker และ dev server:
  - หน้าแรกโหลดไม่มี console error
  - Hero stats ขึ้นค่าจริง ไม่ค้าง NaN/"—"
  - คลิก HARP ในตาราง → risk gauge/attention chart อัปเดตตาม
  - Confusion matrix: ลาก threshold, สลับโมเดล/split, คลิกดู sample, กระโดดกลับ dashboard ได้ครบ
  - A1/A2/A5 ยืนยันแบบ interactive จริงตามตารางด้านบน

---

## 6. หมายเหตุนอกขอบเขตงาน

พบการเปลี่ยนแปลงอื่นในโปรเจกต์ที่ไม่ใช่ฝีมือของงานนี้ (น่าจะเป็นงานคู่ขนานในเทอร์มินัลอื่นของผู้ใช้เอง):
ไฟล์ `.scratch/`, `coop_presentation.pptx`, `numerical_data_examples.xlsx` ถูกลบออกจาก working tree
และ `backend/src/sunseg/tracking/rotation.py` ถูกแก้ (wrap `SkyCoord` ให้ `world_to_pixel` ใช้งานได้ —
ดูสมเหตุสมผล) — session นี้ไม่ได้แตะไฟล์เหล่านี้เลย

## 7. งานที่ยังไม่ได้ทำ

- ยังไม่ได้ `git commit` การเปลี่ยนแปลงใดๆ (ตามคำสั่ง — ไม่ commit เว้นแต่จะถูกขอ)
- `frontend/package-lock.json` เป็นไฟล์ที่มีอยู่ก่อนเริ่มงานนี้ (untracked เดิม) ไม่ได้แตะ
